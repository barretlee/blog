<% if (post.categories && post.categories.length){ %>
	<%- list_categories(post.categories, {
	  show_count: false,
		amount: 2,
	  style: 'none',
	  separator: config.separator || ','
	}) %>
<% } %>
